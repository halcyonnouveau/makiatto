[package]
name = "simple-handler"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = { workspace = true }

[package.metadata.component.target]
path = "wit"

[profile.release]
opt-level = "s"
lto = true
strip = true
